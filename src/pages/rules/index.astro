---
import RuleEmbedList from "@/components/markdown/RuleEmbedList.astro";
import { getCollection } from "astro:content";

const rules = await getCollection("rules");
---

<RuleEmbedList entry_type={"All"} fields={["entry_status"]}/>
